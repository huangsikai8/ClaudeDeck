{
  "name": "claudedeck-probe",
  "displayName": "ClaudeDeck Probe",
  "description": "Throwaway probe: reports what a per-window extension host can see about Claude Code chats.",
  "version": "0.0.1",
  "publisher": "local",
  "engines": { "vscode": "^1.75.0" },
  "activationEvents": ["onStartupFinished"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      { "command": "claudedeck.probe.dump", "title": "ClaudeDeck: Dump Probe Report" }
    ]
  }
}
